DB_HOST=localhost
DB_PORT=5432
DB_NAME=gamedb
DB_USER=gamedb_app
DB_PASSWORD=your_gamedb_app_password_here

SECRET_KEY=change-this-to-a-random-hex-string-min-32-chars

# false — для локальной разработки (HTTP), true — для прода (HTTPS)
SESSION_COOKIE_SECURE=false
